#news.tab-pane.fade.show(role='tabpanel', aria-labelledby='news-tab')
  .container-fluid
    .row.justify-content-center
      .col-lg-7.col-sm-10.text-center.mb-4
        h3.small-font All news post posted here will be avalable for all DoCSoc members to see.
    .row.justify-content-center
      .col-lg-8.col-sm-11
        .card
          .card-body
            h2.card-title Add New News Post:
            form(method="post" action="/sponsor/add-news")
              .row.mt-2.mb-2
                label.col-lg-3.col-sm-4(for="title") Title: 
                input.col-lg-8.col-sm-7(type="text", name="title")
              .row.mt-2.mb-2
                label.col-lg-3.col-sm-4(for="text") Article: 
                textarea.col-lg-8.col-sm-7(type="text", name="text")
              .row.mt-2.mb-2
                label.col-lg-3.col-sm-4(for="link") Learn More Link: 
                input.col-lg-8.col-sm-7(type="text", name="link")
              .row.justify-content-center.mt-2.mb-2
                button.btn.btn-primary.btn-lg.col-10(type='submit') Add
    .row.justify-content-center.mt-4
      .col
        h2.center Your News:
    .row.justify-content-center
      .col-lg-8.col-sm-11
        each news in sponsor.news.reverse()
          .card.mt-3              
            .row
              .col
                .card-body
                  h2.card-title #{news.title}
                  .card-text.story #{news.text}
                  a.card-link(href=news.link) #{news.link}
              .col-2.mt-3
                form(action="/sponsor/remove-news/" + news.date method="post")
                  button.btn(type='submit') Delete 
                    span.octicon.octicon-trashcan   
              