.container-fluid
  .row.align-items-center
    .col-lg-6.col-sm-12.d-inline-block
      img.d-inline-block(src="/static/assets/images/docsoc-sf-logo.svg" width="40" style="vertical-align: sub;")
      h1.d-inline-block.ml-2 DoCSoc Sponsor Portal
    .col-lg-6.col-sm-12
      .row.align-items-center.justify-content-lg-end.text-lg-right
        .col-lg-8.col-sm-9
          -var text = ['Hello There, ', 'Welcome, ', 'Oh hi, ', 'Greetings, ', 'Welcome back, ', 'Good to see you, ']
          h2 #{text[Math.floor(Math.random() * text.length)] + name}
        .col-lg-4.col-sm-3
          form(method="post" action="/logout")
            button.btn.btn-primary.btn-lg.btn-block(type='submit') Logout